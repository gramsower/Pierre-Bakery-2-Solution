@{
  Layout="_Layout";
}

@using Bakery.Models
@model Bakery.Models.Item

<h2>Item Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.ItemName): @Html.DisplayFor(model => model.ItemName)</h3>

@if(@Model.JoinEntities.Count == 0)
{
  <p>This item does not have any flavors added yet!</p>
}
else
{
  <h4>This item has the following flavors:</h4>
  <ul>
  @foreach(FlavorItem join in Model.JoinEntities)
  {
    <li>@join.Flavor.FlavorName</li>
    @using (Html.BeginForm("DeleteJoin", "Item"))
    {
      @Html.Hidden("joinId", @join.FlavorItemId)
      <input type="submit" value="Delete" />
    }
  }
  </ul>
}

<p>@Html.ActionLink("Back to Tag List", "Index")</p>
<p>@Html.ActionLink("Add a Flavor Tag to an Item", "AddItem", new { id = Model.ItemId })</p>
<p>@Html.ActionLink("Edit Item", "Edit", new { id = Model.ItemId })</p>
